# Charger data/cancer/data.csv
# Créer les listes radius_worsts, area_worsts, ids, diagnosis
# Afficher la moyenne des listes

diagnosis = []
radius_worsts = []
for index in range(len(diagnosis)):
    print(diagnosis[index])
    print(radius_worsts[index])

# Bonus moyenne des radius pour diagnosis==0 et diagnosis==1
